# Copy to .env and adjust if needed. Never commit the real .env file.

# Base URL of the Django backend (see backend/.env.example). Defaults to
# http://localhost:8000 when unset, matching `python manage.py runserver`.
VITE_API_URL=http://localhost:8000
